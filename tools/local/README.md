Local Quarantine Area
=====================

Place ad-hoc debug/auth scripts here during development. This folder is
git-ignored by default to avoid committing credentials, tokens, or PII.

Guidelines:
- Never hardcode usernames/passwords or tokens; use environment variables.
- Prefer using the secure credential manager (`mathpdf.secure_credential_manager`).
- Remove logs and screenshots with sensitive information before sharing.

