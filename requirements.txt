# Core
PyYAML>=6.0
requests>=2.31
tqdm>=4.66
PyMuPDF>=1.23
rapidfuzz>=3.5
regex>=2023.12
defusedxml>=0.7
jinja2>=3.1

# UI — the Streamlit cockpit is the sole interface
# (`streamlit run src/ui/cockpit.py`); required for a working install.
streamlit>=1.30

# Async
aiohttp>=3.9
aiofiles>=23.0
aiosqlite>=0.19
httpx>=0.25

# Text processing
langdetect>=1.0
lz4>=4.3
pdfplumber>=0.10

# File system watcher (used by src/watcher/daemon.py)
watchdog>=3.0

# Browser automation (used by src/downloader/eth_institutional.py and cloudflare_session.py)
playwright>=1.40

# HTML parsing (used by publisher downloaders)
beautifulsoup4>=4.12

# Security
cryptography>=41.0
certifi

# LLM (optional — only needed for PDF metadata extraction via local model)
# llama-cpp-python>=0.3

# Testing (install with: pip install -e ".[dev]")
# pytest, pytest-cov, pytest-timeout, pytest-asyncio, hypothesis

# Credential storage for the downloaders (secure_credential_manager.py).
# Imported unguarded and declared nowhere until a manifest check caught it.
keyring>=24.0
# Used by core/utils/type_hints.py, unguarded.
typing_extensions>=4.0
# Complexity metrics for core/optimization/performance_profiler.py.
radon>=6.0
